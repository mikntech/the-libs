import { AppName } from "@failean/shared-types";

const name: AppName = {
  low: "failean",
  up: "Failean",
  special: "fAIlean",
  allUp: "FAILEAN",
  //more after type...
};

// package.name

export default name;
